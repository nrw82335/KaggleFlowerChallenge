This folder stores trained model weights.

Examples include:
- Saved CNN models
- Saved ResNet models
- Checkpoints during training

These files are generated by the training process and should not be manually added.
This folder is ignored by Git to avoid large file uploads.